class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        nums.sort()  # Sort the input list in non-decreasing order
        return nums[len(nums) // 2]  # Return the middle element (majority element)
